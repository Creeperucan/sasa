module.export = {
  data: {
    name: "police"
  },
  async execute(interaction, client) {
    await interaction.edit({
      const response = new MessageEmbed()
          .setColor("GREEN")
          .setTitle(`Cezan Ödendi!`)
    })
  }
}
